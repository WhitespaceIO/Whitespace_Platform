<%= form_for local_assigns.has_key?(:comment) ? comment : Comment.new, :url => url, :remote => true, :html => {id: 'comment-form'} do |f| %>
  <%= f.text_field :text, :placeholder => local_assigns.has_key?(:placeholder) ? placeholder : 'Add comment', :class => 'form-control' %>
<% end %>