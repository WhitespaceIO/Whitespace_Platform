<p id="notice"><%= notice %></p>
<table>
  <tr>
    <th>First Name</th>
    <th>Last Name</th>
    <th>Email</th>
  </tr>
  <tr>
    <td><%= @user.firstname %></td>
    <td><%= @user.lastname %></td>
    <td><%= @user.email %></td>
  </tr>
</table>

<%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
